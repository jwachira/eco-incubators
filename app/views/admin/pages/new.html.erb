<% title 'New Page' %>

<%= simple_form_for(@page, :url => admin_pages_url, :html => {:multipart => :true}) do |f| %>
	<%= render  'form',  :f => f %>
  <%= f.button :submit, 'Create Page' %>
<% end %>