<% title 'Pages' %>

<ul class="pages">
  <% @pages.each do |page| %>
    <li>
      <%= link_to page.title, admin_page_url(page) %>
    </li>
  <% end %>
</ul>

<br><%= link_to "Add New Page", new_admin_page_path %></br>