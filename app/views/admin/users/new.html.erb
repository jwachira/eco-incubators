<div id="login_form">
	<%= simple_form_for(:user, :url => {:action => 'create'}	) do |f| %>
  		<%= f.input :email %>
  		<%= f.input :password %>
  		<%= f.button :submit, t(:sign_in) %>
	<% end %>
</div>

